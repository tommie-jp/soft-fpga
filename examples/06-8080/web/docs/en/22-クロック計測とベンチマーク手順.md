# 22. Clock Measurement and Benchmark Procedures

Procedures for compiling and running BDS C programs placed in `examples/06-8080/` on CP/M.

- **CYCL.C / CYCL.H** — T-state counter shared library (required by all programs)
- **CALIB.C** — Counter calibration (verifies: 4× IN at 10T each = 40T)
- **SORT.C** — T-state comparison between bubble sort and insertion sort
- **BENCH.C** — Measures IN / OUT / memory read-write / addition / function call
- **FREQ.C** — Measures the simulation's equivalent clock frequency and compares with real 8080A (2 MHz)

## 1. Prerequisites

### 1.1 Boot Disks

| Drive | Image | Contents |
|-------|-------|----------|
| A: | `sw/cpm/disks/cpm22.dsk` | CP/M standard commands, R.COM/W.COM |
| B: | `sw/cpm/disks/bdsc.dsk` | BDS C compiler (`CC.COM`, `CLINK.COM`) |

```bash
# Launch from examples/06-8080/ (the R command looks in the current directory)
cd examples/06-8080
./build/cpm
```

### 1.2 Transferring Source Files

Use `R.COM` to transfer files into the running CP/M session.

```text
A>R CYCL.H
A>R CYCL.C
A>R CALIB.C
A>R SORT.C
A>R BENCH.C
A>R FREQ.C
```

> **Alternative (cpmtools)**: Stop `cpm` first, then copy files from the host in one batch.
>
> ```bash
> for f in CYCL.H CYCL.C CALIB.C SORT.C BENCH.C FREQ.C; do
>   cpmcp -f ibm-3740 sw/cpm/disks/cpm22.dsk $f 0:$f
> done
> ```

## 2. CALIB.C — Counter Calibration

### 2.1 Compile

```text
A>B:CC CYCL.C
A>B:CC CALIB.C
A>B:CLINK CALIB CYCL
```

> `CYCL.CRL` only needs to be compiled once and can be reused by SORT.C and BENCH.C.

### 2.2 Run

```text
A>CALIB
```

### 2.3 Expected Output

```text
=== Cycle Counter Calibration ===

cycl_read overhead : ~70 T
empty loop 500x    : ~12500 T  (~25 T/iter)
4x IN loop 500x    : ~32500 T  (~65 T/iter, +40 vs empty)
  => +40 T vs empty (~40 expected)
```

**Interpretation**:

- `+40 T` → IN instruction = 10T each × 4 → counter is working correctly
- The BDS C `for` loop itself takes about 25 T/iter (LXI + DCR + JNZ + INX equivalent)

## 3. SORT.C — Bubble Sort vs Insertion Sort

### 3.1 Compile

```text
A>B:CC SORT.C
A>B:CLINK SORT CYCL
```

(Reuse `CYCL.CRL` compiled in § 2.1)

### 3.2 Run

```text
A>SORT
```

### 3.3 Expected Output

```text
=== Sort Benchmark  N=32 ===

Worst case (reverse):
  bubble sort             : approx 30000 T
  insertion sort          : approx 20000 T

Random data:
  bubble sort             : approx 20000 T
  insertion sort          : approx 13000 T
```

**Interpretation**:

- Insertion sort is about 35% faster in the worst case (reversed input)
- On 8080: swap = 3 × `MOV` = 15T, shift = 1 × `MOV` = 7T — hence the difference

## 4. BENCH.C — I/O, Memory, and Arithmetic Benchmark

### 4.1 Compile

```text
A>B:CC BENCH.C
A>B:CLINK BENCH CYCL
```

### 4.2 Run

```text
A>BENCH
```

### 4.3 Expected Output

```text
=== 8080 Benchmark  N=500 ===

Loop base: 25 T/iter

  4x IN           : 40 T extra (exp 40)
  4x OUT          : 40 T extra (exp 40)
  4x mem rd       : ~28 T extra (exp ~28)
  4x mem wr       : ~28 T extra (exp ~28)
  8x int add      : ~32 T extra (exp ~32)
  C func call     : ~27 T extra (exp ~27)
```

**Interpretation**: A ±2T difference between "exp" and the measured value means the
vm80a RTL T-state implementation is accurate.

| Target | Instruction | Expected |
|--------|-------------|----------|
| `inp()` × 4 | `IN p` × 4 | 40T (= 10T × 4) |
| `outp()` × 4 | `OUT p` × 4 | 40T |
| `char` read × 4 | `MOV A,M` × 4 | 28T (= 7T × 4) |
| `char` write × 4 | `MOV M,A` × 4 | 28T |
| `int` add × 8 | `ADD r` × 8 | 32T (= 4T × 8) |
| C function call | `CALL nn + RET` | 27T (= 17T + 10T) |

## 5. FREQ.C — Simulation Equivalent Clock Frequency

### 5.1 Compile

```text
A>B:CC FREQ.C
A>B:CLINK FREQ CYCL
```

(Reuse `CYCL.CRL` compiled in § 2.1)

### 5.2 Run

```text
A>FREQ
```

### 5.3 Expected Output

```text
=== Equivalent Clock Frequency Ver. 0.1.2 ===
  Measuring : 5000-iter loop (~125 kT)

  Simulated :    8,183,192 Hz
  Real 8080A :    2,000,000 Hz
  vs 8080A  : 409%
```

**Interpretation**:

- `Simulated` — equivalent clock frequency derived from wall-clock time (`FSTA` / `FLAT` ports)
  and the T-state counter
- `Real 8080A` — comparison baseline (fixed at 2 MHz)
- `vs 8080A` — `Simulated / 2 MHz × 100` (Verilator depends on host CPU speed,
  so values of several hundred to several thousand percent are normal)

**Measurement principle**:

```c
freq_start();          // OUT 34h: record wall clock + cycle counter
for (i = 0; i < N; i++) ;  // empty loop, N=5000
freq_latch();          // OUT 38h: finalize Hz from elapsed time
mhz = freq_mhz();     // IN 34h/35h
khz = freq_khz();     // IN 36h
hz  = freq_hz();      // IN 37h
```

> When MHz > 30, `vs 8080A` is rounded to 30,000 kHz to avoid overflowing the 16-bit `int`.

## 6. All-in-One Command Sequence

To compile and run all programs in one batch:

```text
A>R CYCL.H
A>R CYCL.C
A>R CALIB.C
A>R SORT.C
A>R BENCH.C
A>R FREQ.C
A>B:CC CYCL.C
A>B:CC CALIB.C
A>B:CC SORT.C
A>B:CC BENCH.C
A>B:CC FREQ.C
A>B:CLINK CALIB CYCL
A>B:CLINK SORT CYCL
A>B:CLINK BENCH CYCL
A>B:CLINK FREQ CYCL
A>CALIB
A>SORT
A>BENCH
A>FREQ
```

## 7. BDS C Limitations (Caveats)

| Limitation | Details |
|------------|---------|
| No `long` | Only 16-bit `int` — measurement ceiling is 65535 T |
| 8-character identifier limit | `measure_overhead` and `measure_empty` collide; renamed to `do_ovhd` / `do_empty` |
| No `#asm`/`#endasm` | NOP replaced by `inp(0x10)` × 4 to insert 40T |
| Compile extension | `.C` → `.CRL` (intermediate) → `CLINK` → `.COM` |

## 8. Related Documents

- [20-サイクルカウンタ.md](20-サイクルカウンタ.md) — I/O port specification and CYCL.H reference
- [21-サイクル計測プログラム例.md](21-サイクル計測プログラム例.md) — Program content walkthrough
- [15-ファイル構成とファイル交換.md](15-ファイル構成とファイル交換.md) — R command / cpmtools usage
- [18-BDS-C-使い方.md](18-BDS-C-使い方.md) — BDS C compiler details
