# 10. Unix V6 コマンドリファレンス

このシミュレータに収録されている Unix V6 の主要コマンド一覧。
コマンドはすべて `/bin/` または `/usr/bin/` に存在する。

## 1. シェル組み込み

Thompson シェル (`/bin/sh`) の組み込みコマンド。

| コマンド | 書式 | 説明 |
|---------|------|------|
| `cd` | `cd dir` | カレントディレクトリを変更 |
| `exit` | `exit` | シェル終了（ログアウト） |
| `wait` | `wait` | 子プロセスの終了を待つ |

## 2. ファイル操作

| コマンド | 説明 | 使用例 |
|---------|------|--------|
| `ls` | ファイル一覧。`-l` で詳細、`-a` で隠しファイルも表示 | `ls -l /usr` |
| `cat` | ファイル内容をコンソールに出力 | `cat /etc/passwd` |
| `cp` | ファイルコピー | `cp src dst` |
| `mv` | ファイル移動・改名 | `mv old new` |
| `rm` | ファイル削除。`-r` でディレクトリ再帰削除 | `rm file` |
| `mkdir` | ディレクトリ作成 | `mkdir /tmp/work` |
| `rmdir` | 空ディレクトリ削除 | `rmdir /tmp/work` |
| `ln` | ハードリンク作成（V6 にシンボリックリンクはない）| `ln src lnk` |
| `chmod` | パーミッション変更（8 進数指定）| `chmod 755 prog` |
| `chown` | 所有者変更 | `chown root file` |

## 3. テキスト処理

| コマンド | 説明 | 使用例 |
|---------|------|--------|
| `ed` | 行指向テキストエディタ（V6 標準）| `ed file.txt` |
| `grep` | 正規表現検索。`-v` で反転 | `grep root /etc/passwd` |
| `sort` | 行を辞書順ソート | `sort /etc/passwd` |
| `wc` | 行数・単語数・バイト数カウント | `wc /usr/sys/ken/main.c` |
| `head` | 先頭 N 行表示 | `head /usr/sys/ken/main.c` |
| `tail` | 末尾 N 行表示 | `tail /usr/sys/ken/main.c` |
| `diff` | 2 ファイルの差分 | `diff a.c b.c` |
| `pr` | ページ番号付きで整形出力 | `pr file.txt` |

## 4. システム情報

| コマンド | 説明 | 使用例 |
|---------|------|--------|
| `who` | ログインユーザー一覧 | `who` |
| `ps` | プロセス一覧。`-ag` で全プロセス | `ps -ag` |
| `pwd` | カレントディレクトリのパスを表示 | `pwd` |
| `date` | 現在日時を表示 | `date` |
| `df` | ディスク空き容量 | `df` |
| `du` | ディレクトリのディスク使用量 | `du /usr` |
| `size` | 実行ファイルのセグメントサイズ | `size /bin/sh` |

## 5. 開発ツール

| コマンド | 説明 | 使用例 |
|---------|------|--------|
| `cc` | C コンパイラ（Dennis Ritchie 版） | `cc hello.c` |
| `as` | PDP-11 アセンブラ | `as prog.s` |
| `ld` | リンカ | `ld /lib/crt0.o prog.o -lc` |
| `ar` | アーカイブ管理（ライブラリ作成）| `ar tv /lib/libc.a` |
| `nm` | シンボルテーブル表示 | `nm a.out` |
| `od` | バイナリを 8 進ダンプ | `od -c a.out \| head` |
| `adb` | アセンブラデバッガ | `adb a.out` |
| `make` | `Makefile` ベースのビルドツール（V6 付属）| `make` |

### cc の基本的な使い方

```text
# コンパイルのみ
cc -c hello.c       ← hello.o を生成

# コンパイル＋リンク（標準）
cc hello.c          ← a.out を生成

# 実行
./a.out
```

## 6. 電卓・計算

### dc — 逆ポーランド電卓

Thompson が書いた `dc` は Unix V6 を代表するユーティリティ。

```text
dc
  2 3 + p       ← 5
  10 k          ← 小数精度を 10 桁に設定
  355 113 / p   ← 3.1415929203 (π 近似)
  q             ← 終了
```

| コマンド | 説明 |
|---------|------|
| `p` | スタックトップを表示 |
| `q` | 終了 |
| `+` `-` `*` `/` | 四則演算（スタック上位 2 つを演算）|
| `nk` | 小数精度を n 桁に設定 |
| `v` | 平方根 |
| `^` | べき乗 |

### bc — 高水準計算機（dc のフロントエンド）

```text
bc
  scale=5
  sqrt(2)     ← 1.41421
  ^D          ← EOF で終了
```

## 7. ゲーム (`/usr/games/`)

| コマンド | 説明 |
|---------|------|
| `wump` | Wumpus（史上初のテキストアドベンチャーゲームの原型）|
| `ttt` | 三目並べ |
| `chess` | チェス |
| `fortune` | ランダムな格言表示（ディスクイメージによっては未収録）|
| `maze` | 迷路生成 |

## 8. ed — 行指向テキストエディタ

V6 標準の `ed` は行バッファを介してファイルを編集する。

### 起動

```text
# root@unix # ed
a
Hello, Unix V6!
.
p
q
```

### 主なコマンド

| コマンド | 説明 |
|---------|------|
| `a` | 現在行の後ろに追加モード（`.` のみの行で終了）|
| `i` | 現在行の前に挿入モード（`.` のみの行で終了）|
| `p` | 現在行を表示 |
| `l` | 制御文字を見せながら表示 |
| `d` | 現在行を削除 |
| `s/old/new/` | 現在行の最初の `old` を `new` に置換。`g` で全置換 |
| `w file` | ファイルに保存 |
| `q` | 終了（未保存でも `q` を 2 回で強制終了）|
| `1,$p` | 全行表示 |
| `/pattern/` | 正規表現で前方検索 |

## 9. このシミュレータ固有の動作

| 項目 | 内容 |
|------|------|
| ブート | `rkunix` を入力して Unix V6 カーネルを起動 |
| ログイン | `login: root`（パスワードなし）|
| シェル | `/bin/sh`（Thompson シェル）|
| プロンプト | `#`（root シェル）|
| Ctrl+\\ | シミュレーション終了（ブラウザ版では Pause ボタン）|
| Ctrl+C | Unix 側に SIGINT を送る（コマンド中断）|

## 10. 参考

- [52-メモリマップ.md](52-メモリマップ.md) — Unibus デバイスアドレス一覧
- [06-unix-v6-demo-commands.md](06-unix-v6-demo-commands.md) — デモシナリオ
- [Unix V6 Manual Pages（TUHS）](https://www.tuhs.org/cgi-bin/utree.pl?act=get&file=V6/usr/man)
