MEMORY {
  ZP:  start = $0080, size = $0040, type = rw;
  RAM: start = $0300, size = $0D00, file = %O;
}
SEGMENTS {
  ZEROPAGE: load = ZP,  type = zp;
  STARTUP:  load = RAM, type = ro;
  CODE:     load = RAM, type = ro;
  DATA:     load = RAM, type = rw;
  BSS:      load = RAM, type = bss;
}
